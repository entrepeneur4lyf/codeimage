interface:
  display_name: "Codeimage"
  short_description: "Repo-specific patterns and workflows for codeimage"
  default_prompt: "Use the codeimage repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true